#include "main.h"
/**
* _islower -function that computes the absolute value of an integer
* @n: the int to  checks
* Return: the absolute value of int
*/
int _abs(int n)
{
if (n > 0)
{
return (n);
}
return (-n);
}
