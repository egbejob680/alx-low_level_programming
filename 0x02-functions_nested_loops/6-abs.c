#include "main.h"
/**
* _islower -check for lower case character, followed by a new line
* @n: the int to  checks
* Return the absolute value of int
*/
int _abs(int n)
{
if (n > 0)
{
return (n);
}
return (-n);
}
