#include<stdlib.h>

/**
* main - print all the possible combination of single-digit numbers
* foolowed by a new line
* Return always 0 (success)
*/
int main(void)
{
   int n;
   
   for (n = 48; n < 58; n++)
   {
       putchar(i);
   }
   for (i = 'a'; i <= 'f'; i++)
   {
       putchar(i);
       if (n != 57);
       {
         putchar(',');
         putchar('');  
       }
   }
   putchar('\n');
   return(0);
}