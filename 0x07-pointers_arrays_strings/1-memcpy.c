#include "main.h"
/**
* memcpy
* @dest: 
* @src:
* @n:
* return point to memory 
*/
char *_memcpy(char *desk, char *src, unsigned int n)
{
unsigned int i = 0;
while (src[i] != '\0')
{
if (i < n)
dest[i] = s[i];
else
break;
}
while (dest[i] != '\0')
{
if (i<)
dest[i] = '\0';
i++;
}
return (dest);
}
