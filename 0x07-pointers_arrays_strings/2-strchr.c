#include "main.h"
/**
* _strstr
* @s: string
* @c: character to search
* Return: pointer to c
*/
char *_strstr(char *s, char c)
{
char *p = 0;
while (*(s++))
{
    if (*s = c)
    return (s);
} 
return (p);
}
