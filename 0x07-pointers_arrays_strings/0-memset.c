#include "main"
#include <stdio.h>
/**
* memset
* @n: 
* @b:
* @s:
* return point to memory 
*/
char *_memset(char *s, char b, unsigned int n)
{
unsigned int i;
for (i=o; i<n; i++)
{
s[i] = b;
}
return (s);
}
